# Smashq — Sprint Backlog

> **Single Source of Truth**: GitHub Issues — https://github.com/ARO-LABS/smashq/issues
> **Board (cross-project)**: https://github.com/orgs/ARO-LABS/projects/1 (ARo-LABS | KanBan)
> **Release-Historie**: `CHANGELOG.md`
> **Langfristige Roadmap**: `Softwareprozess/arc42-specification.md`, Abschnitt 1.1 "Roadmap-Vision"
>
> Regel: **1 aktive Phase + 1 nächste Phase. Mehr nicht.** Alles weitere → Backlog (Bullets, keine Pläne).

## Aktuelle Phase

- **Issue-Abarbeitung + Feature-Freeze-Ende (2026-07-20/21, ultracode):** Feature-Freeze per Maintainer-Entscheid beendet — neue Feature-Politik in CLAUDE.md (Auflagen: ausgiebige Tests + Design-System-Harmonie; unterspezifizierte Issues → Rückfrage-Kommentar + Backlog). 7 PRs parallel erstellt (je isolierter Worktree, Gates + adversarisches Review pro PR). **Stand Merge-Welle 2026-07-21: #41–#45 gemergt** (je mit Multi-Agent-Review + Review-Nacharbeiten in dieser Session): **#41** Signing-Prep release.yml (Refs #22, No-op ohne Secrets, Triple-Check nachgewiesen), **#42** Proxy-Doku README (Refs #25), **#43** Dependabot-Highs vite→6.4.3 + overrides (#16; linkify-it/form-data waren stale Alerts), **#44** Session-Neustart-Icon (#13, Semantik: frische Session ohne --resume), **#45** Pin-Test-Tautologien mit Negativ-Beweisen (#18; vierter tautologischer Test `close_session_cancels_watcher` direkt im PR miterledigt). **#46** Auto-Update-Toggle Default AN (#21, geschützter Pfad, Schema v14, settingsSync-Broadcast + Roundtrip-Tests) — vorbereitet, wartet auf User-Smoke (Triple-Check Schritt 3); **#47** Kanban-Auth »Im Terminal öffnen« (Allowlist-Diskriminator) + gh-Auth-Preflight im System-Panel (#38) — in Review/Merge-Queue. 11 leere Issues (#23/#24/#26–#34) mit konkreten Rückfrage-Kommentaren versehen → warten im Backlog; #37 bleibt upstream-blockiert (wayland-scanner 0.31.10 pinnt quick-xml ^0.39, Status-Kommentar gesetzt). **Offen:** visuelle Smokes (#44 Icon-Leiste, #46 Toggle + Updater-Smoke, #47 Terminal-Öffnen Win/macOS); #46 nach User-Smoke mergen, #47 nach Review mergen.
- **Dependabot-High-Alerts (#16) — gemergt (PR #43, 2026-07-21):** `vite` ^5.3.0 → ^6.4.3 inkl. `overrides` (`"vite": "$vite"`), damit auch die von vitest 2.1.9/vite-node/@vitest/mocker genesteten Kopien (deps deklarieren `vite ^5`) auf die gepatchte 6.4.3 dedupen — schließt GHSA-fx2h-pf6j-xcff (high, `server.fs.deny`-Bypass Windows) + GHSA-v6wh-96g9-6wx3 (launch-editor NTLMv2) + esbuild-Moderate. `linkify-it` 5.0.2 und `form-data` 4.0.6 waren im Lockfile bereits ≥ Patch-Version (Alerts #19/#11 stale, schließen beim nächsten Dependabot-Rescan). `npm audit`: high 1→0, moderate 3→0; **verbleibend 2 Criticals** (vitest ≤3.2.5, GHSA-5xrq-8626-4rwp) → nur via vitest-Major fixbar, bewusst ausgeklammert (Backlog-Item unten).
- **Kanban-Auth: „Im Terminal öffnen" + gh-Auth-Preflight (#38) — PR offen (`feat/issue-38-kanban-auth-terminal`, 2026-07-20):** Erstes Feature nach Ende des Feature-Freeze. (A) Neuer Tauri-Command `open_system_terminal` (`github/auth.rs`): öffnet das System-Terminal (Windows: `wt` falls auf PATH, sonst `cmd /K`; macOS: osascript → Terminal.app; Linux: `x-terminal-emulator` best effort) mit dem Fix-Befehl. Sicherheitsmodell nach `PermissionMode`-Muster: Frontend sendet nur den Diskriminator (`gh_login` | `gh_refresh_project_scope`), Rust mappt auf `&'static str`-Literale — unbekannte IDs → strukturierter Validation-Error. Button an Kanban-Fehlerkarte + Board-Picker (nur wenn `terminalCommandId` gesetzt). (B) `check_gh_auth_status`: parst `gh auth status` (stdout+stderr kombiniert, gh wechselte den Stream zwischen Versionen; KEIN `--show-token`) → `{loggedIn, host, account, scopes, hasProjectScope}`; System-Panel zeigt Sektion „GitHub-Konto" mit Scope-Warnung + `CopyableCommand` + Terminal-Button. `CopyableCommand` aus `KanbanBoard.tsx` nach `shared/` extrahiert (Review-Follow-up g teilweise adressiert). **Review-Nacharbeit (2026-07-21, 8 Findings + 1 optional, alle umgesetzt):** Parser TDD-gehärtet — Offline/Proxy-Fehler wird nicht mehr als „Nicht angemeldet" fehldiagnostiziert (nur kanonische Logout-Phrasen → loggedIn:false, sonst `classify_gh_error` → retryable ServiceTimeout), github.com-Block bevorzugt (GHES-Multi-Host), Scope-Parsing an den Block gebunden (kein Bleed); osascript-Escaping konstruktiv, Unix-Zombie-Reaper, `timed_output`-Fehler klassifiziert, `CopyableCommand`-Timer-Cleanup (+ neue Testdatei), `OpenInTerminalButton` auf `ui/Button`, Spiegel-/Guard-Kommentare. Windows-Terminal-Smoke auf Dev-Maschine bestanden (wt-Zweig, exakte Code-Konstruktion). **Offen:** visuelle Smoke System-Panel/Fehlerkarte in installierter Build; Terminal-Öffnen-Smoke macOS/Linux (kein Gerät).
- **Release v1.0.24 — veröffentlicht (2026-07-20):** Tag `v1.0.24` auf User-Go gepusht, `release.yml` komplett grün (build-windows, build-macos signiert+notarisiert, refresh-design-doc). Release-Notes gesetzt, `latest.json` verifiziert (version 1.0.24, windows-x86_64 + 4 darwin-Keys, alle Signaturen vorhanden). `whatsNew`-Rumpf 1.0.25 geseedet (Vergessens-Schutz). Sammelte PRs #39 + #40 + Dep-Bump (`@tauri-apps` ^2.10.1). **Offen:** Update-Smoke 1.0.23→1.0.24 via installierter .exe (v-Badge → „Update verfügbar" → Installieren → Neustart) durch einen Bestands-User bestätigen.
- **Settings-Fenster-Persistenz — gemergt (PR #40, 2026-07-16):** User-Report „Einstellungen speichern nicht" (Permission-Modus sprang zurück). Root Cause: Settings-View rendert NUR in `DetachedViewApp` (immer Sekundärfenster); `tauriStorage` verwirft deren Disk-Writes still (M-01-Guard) — Broadcast ist der einzige Persistenz-Pfad, aber 5 Setter (`setDefaultShell`/`setDefaultPermissionMode`/`setDefaultProjectPath`/`setNotifications`/`setSound`) broadcasteten nie → Werte erreichten die Platte seit v1.0.0 nicht; „Gespeichert"-Indikator ist optimistisch (Store-Subscribe, kein Disk-Ack). Fix: `settingsSync`-Broadcast-Variante + `applySettingsSync`-Empfänger (`wireRuntimeGates`, raw setState, Trust-Boundary-Sanitizing) + Setter-Input-Sanitize + Roundtrip-/Heal-Integrationstests. Gates: 2520 vitest, tsc, Build, CI grün. Review (8 Finder + 8 Verifier): 0 Blocker, 7 Findings → Backlog-Eintrag. Siehe `lessons.md` 2026-07-16.
- **Kanban-Auth: Fix-Befehl kopierbar — gemergt (PR #39, 2026-07-16, Zwischenschritt zu #38):** `GithubErrorInfo.command` strukturiert statt Befehl im Hint-Fließtext; `CopyableCommand` (Mono-Box + Copy-Button) in Fehlerkarte + Board-Picker. Review (4 Finder, verifiziert): 0 Blocker; Findings (Copy-Mechanik 3× dupliziert, Icon-/Token-Abweichungen, Picker-Pfad ungetestet) → Backlog-Eintrag.
- **CI-Rot-Fix nach master — flaky Tab-Test + frische cargo-audit-Advisories (`fix/ci-security-audit-and-flaky-tabs-test`, 2026-07-14):** master war doppelt rot, ohne Code-Change (PR-#35-Tree byte-identisch, PR-Branch war grün). (A) **CI/Tests:** `ConfigPanelTabList`-Test flaky — `waitFor`-Waypoint („Settings sichtbar") ist wegen Anti-Flash (`presence===null` → alle Tabs) auch im Ladezustand wahr, gatet nicht auf Auflösung → langsame Runner returnen zu früh, „Hooks" noch da. Fix: negative Assertion INS `waitFor` (2 Tests). Deterministisch reproduziert (Scratch: Presence nie auflösend). (B) **Security-Audit:** 5 frische 2026er RUSTSEC-Vulns → crossbeam-epoch → 0.9.20, plist 1.8→1.10 (quick-xml 0.41.0, tauri unberührt); wayland-scanner-quick-xml-0.39 (Build-Codegen, trusted XML, per Lock unfixbar) via dokumentiertem `--ignore` + Tracking-Issue. Gates lokal grün: 2508 vitest, cargo check/clippy/fmt, `cargo audit` exit 0. Siehe `lessons.md` 2026-07-14 (×2). **Offen:** CI grün auf dem PR bestätigen; Tracking-Issue für den quick-xml-Ignore anlegen.
- **Settings „Über"-Section — gemergt (PR #35, 2026-07-10):** Neue Settings-Kategorie „Über" (letzter Nav-Eintrag) mit App-Version (`getVersion()`), Build-Commit (`__GIT_HASH__`, klickbar → GitHub-Commit), Build-Datum (`__BUILD_DATE__`), Plattform (neuer Zero-Dep-Rust-Command `get_os_info` → `{os, arch}` aus `std::env::consts`, gespiegeltes `check_prerequisites`-Muster) + „Diagnose kopieren" (Clipboard-Block für Bug-Reports) + Repo-/Issues-/Releases-Links (`open()`). Bewusst KEIN `plugin-os` (Backend = Plattform-Autorität, null neue Deps, keine Capability-Änderung), KEIN Updater-Pfad. Subagent-driven (3 Tasks, zweistufiges Review + opus-Branch-Review „Ready to merge"). Gates: 2508 vitest + 102 Integration, 569 cargo, tsc/build grün. **Offen:** visuelle Smoke (Settings → Über: Werte korrekt, „Diagnose kopieren", Links öffnen) im lokalen Build.
- **Kanban-Auth-Fix (#7) + In-App Prerequisite-Check (#10) — gemergt (PR #17/#19, 2026-07-09):** Parallel in zwei Worktrees (subagent-driven, zweistufiges Review pro Task + finaler Branch-Review). **PR #17** (`fix/issue-7-kanban-auth`): Retry-Button lud bei fehlender Board-Auswahl nie die Projektliste neu (loadBoard-No-op → „Auth erst nach Refresh"); Owner-Wechsel verschluckte klassifizierte Fehler als leere Liste („ARO-LABS zeigt keine Projekte") → neuer `pickerError`-State rendert inline im Chooser; `gh project list --limit 100`. Gates: 179 Kanban-Tests, tsc, Build, cargo 48/48. **PR #19** (`feat/issue-10-prerequisite-check`): neues `prerequisites.rs` (`check_prerequisites`: claude/git/gh/Shell → found+path), `ensure_claude_available`-Guard in `create_session` (ADPError `TerminalSpawnFailed` + details `"claude_missing"`, Muster `gh_missing`), Frontend-Classifier + plattformspezifischer Install-Hint (macOS `.zprofile`), Settings-Panel „System", Restore-All-Failed-Toast. Panel + Guard teilen `which_executable` als Single Source of Truth. Gates: 2506 vitest, 96 Integration, 564 cargo, clippy. Follow-up: Issue #18 (drei alte tautologische Pin-Tests). Sichtbarkeits-Klärung (Analyse 2026-07-10): Org-Mitgliedschaft reicht für Board-Sichtbarkeit (`viewer.organizations` + `gh project list --owner`); reine Projekt-Collaborator ohne Org-Mitgliedschaft bleiben strukturell außen vor (gh bietet kein „Projekte, an denen ich mitarbeite"-Listing). Follow-up-Kandidat: SAML-SSO-Fehler matcht keine Forbidden-Phrase in `classify_gh_error` → fällt auf `unknown` statt eigenem Hint. **Offen:** visuelle Smoke (Kanban-Fehlerkarte, System-Panel) in installierter Build.
- **Session-Farbige Layout-Mini-Map — implementiert (`fix/issue-9-session-tile-color`, 2026-07-09):** Die Grid-Mini-Map in der Session-Kachel (`SessionCard.tsx`) faerbte die aktive Zelle immer fest ueber `bg-accent` (azurblau), unabhaengig von der zugewiesenen Session-Farbe oder Status (#9). Fix: die aktive Zelle uebernimmt jetzt inline `dotBackground` — dieselbe Farbe wie der Status-Punkt der Kachel (inkl. Fehler-/Wartet-Zustand), inaktive Zellen bleiben `bg-neutral-600`. Tests: Happy-Path (aktive Zelle == Punkt-Farbe bei normalem Status) + Edge-Case (folgt in den Fehler-Zustand, inaktive Zellen bleiben neutral ohne Inline-Farbe). CHANGELOG unter Behoben ergaenzt. PR #15 gemergt (`a0345a8`, CI gruen: Tests + Rust Win/macOS), Issue #9 geschlossen, Branch geloescht. **Offen:** visuelle Smoke (Mini-Map-Farbe) beim naechsten installierten Build.
- **Default Permission Mode für neue Sessions — gemergt (PR #14, 2026-07-09):** Bisher startete der einzige claude-Start-Pfad (`shell_args`) fest mit `--dangerously-skip-permissions` — kein Weg, den Permission-Modus zu wählen. Umgesetzt in 4 Tasks: (1) Rust `PermissionMode`-Enum (Standard/Auto/Plan/Bypass) durch `shell_args`/`create_session` — Kommandozeile nur aus `&'static str`-Literalen, kein Roh-Text; (2) `settingsStore.defaultPermissionMode` + v13-Migration (Default `standard`, unbekannte Werte fallen auf den sichersten Wert zurück); (3) Mode-Select in `NewSessionDefaultsPanel` (Einstellungen → Sessions); (4) `useSessionCreation` reicht den Modus durch alle 3 `create_session`-Invokes (neu + Resume). **Verhaltensänderung:** neue Sessions starten jetzt standardmäßig im Modus Standard (Nachfragen) statt bisher YOLO — im CHANGELOG unter Geändert dokumentiert. **Offen:** visuelle Smoke des neuen Selects im lokalen Build.
- **Release v1.0.23 (2026-07-08) — „Was ist neu"-Modal + gesammelte Fixes seit v1.0.21:** v1.0.22 wurde nie getaggt; v1.0.23 released die 82 Commits seit v1.0.21. Whats-New-Feature: Template/Content-Trennung — `src/whatsNew.ts` (kuratierte Highlights + Watchouts pro Version, Icons als geschlossene `WhatsNewIconKey`-Union auf die ICONS-Registry gemappt), `useWhatsNew` (Gating: `getVersion()` vs. persistiertes `lastSeenVersion` v12, Stempel beim ANZEIGEN, Erstinstallation/fehlender Eintrag = stiller Skip), `WhatsNewModal` (ui/Modal-Basis, Watchout-Box, Changelog-/Feedback-Links). Mount in `App.tsx` — bewusst NICHT AppShell (geschützter Updater-Pfad). Subagent-Review aller 82 Commits ergänzte im CHANGELOG [1.0.23]: macOS-Session-Start-Fix, Protokolle-Redesign (Scope/Sort, Cross-Window-Clear, Papierkorb truncatet Datei), Drag-Reorder-Persist, Rename-Space-Fix, Sicherheits-Rubrik (dompurify/markdown-it). **Hinweis:** „v1.0.3"-Wunsch war semver-kleiner als 1.0.21 → 1.0.22 → final 1.0.23. Pro künftigem Release: `whatsNew.ts`-Eintrag zusammen mit CHANGELOG pflegen (fehlt der Eintrag → kein Modal, bewusste Redaktions-Entscheidung). **Offen:** Release-CI validieren (latest.json mit darwin-Keys, erster signierter macOS-Build), Update-Smoke 1.0.21→1.0.23 in installierter .exe.
- **Session-Identity-Fixes — in `master` gemergt + gepusht (`43fccc3`, 2026-07-08):** Zwei User-Reports, ein architektonischer Kern: das asynchrone Mapping interne Session-ID ↔ Claude-CLI-UUID (Discovery). (1) **Falsche Session beim Restore** (`698126f` + `7898938`): Der Restore-Scan-Fallback nahm ohne persistierte UUID die *neueste* Session im Projektordner — jetzt persistiert `RestorableSession.createdAt` als Zeitanker und der Fallback nutzt `pickBestHistoryMatch` (started_at-Nähe, wie Live-Discovery); ohne Match wird frisch gestartet statt geraten. Dazu Rust-Watcher deterministisch (`diff_new_uuid`: emit nur bei EXAKT einer neuen jsonl, bei Ambiguität Scan-Fallback) + Claim-Check in `onResolvedEvent` (UUID einer anderen Session wird verworfen). (2) **Rename erreichte die History nicht** (`e174144`): Rename vor UUID-Auflösung strandete in `pendingTitleOverrides` (History liest nur `sessionTitleOverrides[uuid]`) — `commitRename` triggert jetzt einen One-Shot-Anchored-Resolve (`resolveClaudeIdByAnchor`) und flusht bei Treffer sofort. Gates grün: tsc, 2472 vitest + 88 Integration, 538 cargo test, clippy, npm build. **Offen:** manuelle Smoke in der lokal installierten Build (2 Sessions selbes Projekt → App-Quit → Restart resümiert die richtigen; Rename direkt nach Session-Start erscheint in Config-Panel-History). **Restrisiko dokumentiert:** zwei ~gleichzeitige Fresh-Spawns im selben Ordner bleiben heuristisch (Zeitanker ≈ identisch) — echte Determinismus-Option wäre `claude --session-id <uuid>` beim Spawn (SmashQ vergibt die UUID selbst), als Härtungs-Kandidat im Backlog.
- **macOS-Auto-Updater aktiviert — `feat/macos-updater` in `master` gemergt (`cd02723`, 2026-07-08):** Bisher baute `build-macos` nur eine unsignierte DMG ohne Updater-Artefakte → Mac-User hatten keinen Update-Pfad, `latest.json` hatte keine `darwin-*`-Keys. Umgesetzt (ohne Mac-Gerät, Signierung läuft im CI-Runner): Apple **Developer ID Application**-Cert erzeugt (CSR via openssl, Cert-Download via Browser, `.p12` lokal gebaut — Key/`.p12` nur im Scratchpad), 7 GitHub-Secrets (`APPLE_CERTIFICATE`/`_PASSWORD`, `APPLE_SIGNING_IDENTITY`=`Developer ID Application: Hovsep Aroyan (2V9XACPAJ5)`, `APPLE_TEAM_ID`, `APPLE_ID`, `KEYCHAIN_PASSWORD`, `APPLE_PASSWORD`). `release.yml`-`build-macos`: Cert-Import-Step + tauri-action mit Minisign-(Updater-)+`APPLE_*`-Env, `--bundles app,dmg`, `includeUpdaterJson: true` → merged `darwin-aarch64`/`-x86_64` in die von `build-windows` angelegte `latest.json` (tauri-action fetcht+merged, Windows bleibt). **Offen:** (1) User-Fix des Secret-Tippfehlers `APPLE_PASSWORT`→`APPLE_PASSWORD` (sonst signiert-aber-nicht-notarisiert); (2) erster Release-Tag validieren (`latest.json` hat darwin-Keys + `.app.tar.gz`+`.sig`); (3) realer macOS-Update-Smoke (braucht Gerät/VM/Tester — User hat kein Mac); (4) Haken: unsigniert→erstes signiertes Update kann bei Bestands-Usern einmalig fehlschlagen, danach sauber. Siehe `lessons.md` 2026-07-08.
- **Design-System-Remediation + visuelles Feedback — lokal in `master` gemergt + gepusht (`fix/design-remediation` → FF, 2026-07-07):** 4-Subagenten-Audit fand Token-Leaks; alle 6 Phasen umgesetzt: (1) globaler Akzent cyan-teal → **azure Hue 230** (`--accent-h`, `ACCENT_HUES` cyan raus, Persist-Migration Legacy-„cyan"→„azure" in migrate + onRehydrate); (2) Token-Kette via Tailwind (Spacing/Motion/Typo/`surface-inset`/diff/`cat-*`) — **kritischer Fix:** `alpha()`-Helper wickelt var-Farben in `color-mix(... calc(<alpha-value>*100%) ...)`, sonst droppt Tailwind Opacity-Modifier auf plain-var-Farben still zu transparent; (3) rohe Palette → Tokens (~15 Dateien); (4) Glow var-basiert; (5) Icon-Registry erzwungen (45 Dateien → `ICONS.*`, ESLint `no-restricted-imports` verbietet lucide); (6) xterm theme-fähig (Canvas-Rasterisierung, da xterm kein oklch parst). **Visuelles Feedback (selber Branch):** #1 Grid-Mini-Map Ruhe-/Hover-Kollision (SessionCard, `mr-4` + `group-hover:opacity-0`); #3 Dark-Mode-Kontrast „Lifted Charcoal" (`.dark`-Ramp gehoben, base L 8%→18% — OKLCH→sRGB-Gamma stauchte das schwarze Ende zu rgb 2/5/11 = base↔raised 1.02:1; jetzt 1.09–1.15:1, **alle Werte live via Playwright gegen WCAG gemessen**); #2 Kanban-Modal-Ränder (Surface-Token→Border-Token); #4 Tasks-View-Redesign (2 Header-Bars→1 B-bar, Projekt-Scope-Dropdown + „Ansicht"-Popover mit Gruppierung/Sortierung, Grid-Tile/Window „In großer Ansicht öffnen"-Affordanz). Gates grün: tsc, 2453 vitest, Build, test:coverage-Ratchet. **Offen:** visuelle Smoke der installierten `.exe` durch User (azure überall, Dark-Mode-Surfaces trennbar, Tasks-Toolbar); Backlog-Reste (voller `border-neutral-800`-Audit, ICON_SIZE-Enforcement, Chunk-Splitting codemirror/xterm >500kB).
- **Mac-Support Runde 2 — Session-Start-Blocker + GUI-PATH gefixt (`fix/macos-session-start`, 2026-07-03):** Realer Mac-Smoke aus Runde 1 (Eintrag unten) zeigte: Sessions starteten auf macOS trotz plattformbewusster Shell-Aufloesung nie. Root Cause (5-Finder-Audit, adversarisch verifiziert): `addFavorite` stempelte JEDEN Favoriten mit `shell:"powershell"` → QuickStart schickte "powershell" → `resolve_shell_pref` behielt es plattformuebergreifend → `pwsh` (auf Standard-Mac nicht da) → `create_session` scheiterte; `handleQuickStart` schluckte den Fehler (nur `logError`, kein Toast) → Klick tat sichtbar nichts. Fix (Defense-in-Depth, 5 Ebenen): (1) `resolve_available_shell` (manager.rs) faellt auf Plattform-Default zurueck, wenn das Ziel-Exe nicht auf PATH ist — pwsh bleibt fuer Mac-User die es installiert haben; (2) `hydrate_path_from_login_shell` (util.rs/lib.rs) laedt beim Start den Login-Shell-PATH ins Prozess-Env, damit `gh`/`git`/`node` im Finder-gestarteten .app gefunden werden; (3) Favoriten defaulten auf `"auto"` (settingsStore), Typ geweitet; (4) Error-Toasts in `handleQuickStart` + `handleResumeSession` statt stillem `logError`; (5) `windowsPty` xterm-Hint hinter `isWindows()` (neue `utils/platform.ts`). Gates gruen: 489 cargo test (+5 `available_shell`, +3 `merge_paths`), clippy/fmt, tsc, 2419 vitest + 9 Integration (macOS-Silent-Fail-Regression-Guard), npm build. Ground-Truth: `tauri dev` startet, Log zeigt „Hydrated PATH from login shell (27 entries)" — **im laufenden .app verifiziert**: Session-Start mit Legacy-„powershell"-Pref fiel auf zsh zurueck und spawnte erfolgreich (Log „falling back to platform default 'zsh'" → „spawned successfully"). Dazu neuer CI-Guard `rust-macos` (`.github/workflows/ci.yml`, `macos-latest`: `clippy --all-targets` + `cargo test` ohne die 4 Finder-abhaengigen `trash`-Tests) — faengt macOS-`cfg`-Compile-/Logik-Regressionen pre-merge (schliesst Audit-Finding „CI testet nie macOS"). **Bewusst NICHT angefasst:** `bundle.targets:["nsis"]` (lokaler mac-Build braucht `--bundles dmg`; Aenderung beruehrt geschuetzten Updater/Release-Pfad); macOS-Signing/Notarization + macOS-Updater brauchen Apple-ID.
- **Rechtsklick-Projektfarbe + natives Kontextmenü — Release v1.0.21 (2026-07-02):** Bug: Session-Farbmenü öffnete nur bei erkannter `claudeSessionId`; Favoriten hatten gar kein Menü (natives WebView-Menü leakte). Fix: Akzent auf `folderAccents` (Ordnerpfad-Key, immer vorhanden) umgestellt — geteilt zwischen Favorit + Sessions desselben Ordners; Auflösung Ordner-Override → Legacy-Per-Session → Hash. FavoriteCard bekommt dasselbe `SessionAccentMenu`; Grid-Rahmen + Preview erben die Farbe. Globaler `contextmenu`-Guard in `wireRuntimeGates` (jedes Fenster) unterdrückt das native Menü außer in Textfeldern. TDD: tsc, 731 vitest (touched suites), build, eslint grün. **Anmerkung:** die frühere Phasen-Notiz „`folderAccents` implementiert + gepusht" war Doc-Drift — der Code hatte es nicht; hier real umgesetzt. **Offen:** visuelle Smoke in der installierten `.exe` (Rechtsklick Session + Favorit, Farbe färbt Ordner überall, Textfeld-Copy/Paste bleibt).
- **Mac-Support + UX-Feedback + Projekt-Akzente — implementiert + gepusht (2026-07-02):** Erster Mac-User-Report: Sessions starteten auf macOS nie. Fix: plattformbewusste Shell-Aufloesung im Rust-Backend (`ShellPlatform`-Enum, `auto` → powershell/zsh/bash, Unix-Shells als Login-Shell `-l -c` wegen GUI-PATH), neuer Command `detect_shells` — Settings bieten nur real installierte Shells an; Shell-Preference geht roh ans Backend (Frontend-Mapping entfernt). Dazu: Save-Feedback-Indikator im Einstellungen-Header („Speichern…" → „✓ Gespeichert"), `Tooltip`-Komponente ersetzt natives `title` im Dock (300 ms statt ~1 s Browser-Delay), Per-Projekt-Akzentfarbe `folderAccents` (Favorit + Sessions desselben Ordners teilen die Farbe; Validation in migrate + onRehydrateStorage). Gates gruen: 2417 vitest, 481 cargo test, clippy, `npm run tauri build` (Signing-Key nur in CI). **Offen:** realer macOS-Smoke durch den Mac-User (Session-Start, claude via zsh-Login-PATH); Updater-Smoke in installierter `.exe` (Dock wurde beruehrt — reines Markup); macOS-Aequivalent zum win_job-Prozessbaum-Kill fehlt (Backlog).
- **Library- + Farb-Polish — implementiert (lokal in `master`, gepusht 2026-07-02):** Vier Commits aus dem Library-Review: Library blendet Projekte ohne Konfiguration aus (Fussnote mit Anzahl, geteilter `hasScopeContent`-Helper im configDiscoveryStore); Hook-Karte öffnet den Detail-Modal mit voller Command-Ansicht; Memory-Dateien löschbar via Papierkorb-Icon + Inline-Bestätigung (neuer Tauri-Command `delete_user_claude_memory_file`, Segment-Whitelist `projects/<dir>/memory/<file>`, `trash::delete`); redundantes Favoriten-Label-Suffix entfernt; Grid-Zellen-Rahmen 2px/3px theme-bewusst (`accentFrameColorFor` mit `--accent-l`/`--accent-c`); ConfigPanel/FavoritePreview erben die Session-Farbe via `--accent-h`-Override, aktive Tabs `bg-accent-a30`. **Offen:** manuelle Smoke durch User (Library, Grid-Rahmen, Panel-Farben).
- **Whole-Tile-Drag — lokal in `master` gemergt, gepusht 2026-07-02:** Session-Kacheln, Favoriten-Karten und Favoriten-Gruppen sind per Klick-und-Ziehen auf der ganzen Kachel verschiebbar; Grip-Handles komplett entfernt (inkl. verwaistem dragHandle-Icon). Mechanik: dnd-kit-Listener auf dem Sortable-Root (Gruppen: Header-Zeile wegen Bubbling), neuer geteilter `SmartPointerSensor` (ignoriert button/a/input/textarea/select/[data-no-dnd], behält isPrimary+button===0-Guards), 6px-Aktivierungsdistanz trennt Klick von Drag. Dazu: Collapsed-State der Favoriten-Gruppen persistiert (`492ddf8`). Gates grün: tsc, 132 Testdateien vitest, eslint, npm build. **Offen:** visuelle Smoke durch User (Drag überall, Klick/Doppelklick/Rechtsklick/Buttons/Chevron/Rename-Input).
- **Logging-Hardening — lokal in `master` gemergt, gepusht 2026-07-02:** 34-Agenten-Armada-Review des NDJSON-Logging-Features → 24 verifizierte Findings (3 High). Gefixt in 9 Commits: logError(undefined)-Crash, Persistenz-Gate vom Master-Gate entkoppelt + Pre-Wiring-Buffer, Rust-Write-Fehler propagiert (append_frontend_logs kein Silent-Ok mehr) + Input-Caps, Read-Pfad UTF-8-robust/rotations-übergreifend/async, Close-Flush-in-flight-Race, Listener-Races (cancelled/disposed-Flags), Cross-Window-Log-Sync (Broadcast + Snapshot — Split-Brain-High), sanitizePreferences in beiden Hydration-Hooks, Dead Code (parseBackendLogLine, open_log_window). Gates grün: tsc, 2373 vitest, npm build, cargo fmt/clippy/test (514). **Offen:** manuelle Smoke (Protokolle-Fenster sieht Hauptfenster-Fehler live + beim Mount) durch User.
- **Kanban-Vereinfachung — implementiert (`feat/kanban-single-board`, lokal in `master`, 2026-06-09):** Ein einziges global gewähltes Board (Picker statt Global/Projekt-Toggle). `KanbanDashboardView` gelöscht, Kanban öffnet als eigenes Fenster (`DetachedViewApp` → `<KanbanBoard />`). Kanban-Tab aus der Session-Config-Sidebar entfernt → Folder-Mode + `projectByFolder` entfallen, `KanbanBoard` global-only, `projectStore` auf nur `globalProject` reduziert (v1→v2-Migrate). Gates grün: 2347 vitest, tsc, build, eslint. **Phase C (Auto-Discovery pro Favorit) verworfen** — Modell „ein manuell gewähltes Board" ersetzt es bewusst. **Offen:** `.exe`-Smoke durch User.
- **Kanban-Overhaul Phase A+B — implementiert (`feat/kanban-overhaul`, lokal in `master` gemergt, 2026-06-09):** Org-Boards als globales Board ladbar (GraphQL `node(id:)` statt viewer-scoped `projectV2(number)`), `list_user_projects(owner)` + neuer `list_project_owners`-Command (du + Orgs), Owner-Dropdown im Picker. Ehrliche Fehler: `classify_gh_error` (Rust) → strukturierte ADPErrorCodes mit `details`-Diskriminator; `classifyGithubError` (TS) ersetzt das fragile `message.includes("project")`. Stale-Selection-Recovery (gelöschtes Board → Chooser statt Sackgasse/stiller Auto-Jump). `projectStore` mit `version`/`migrate`/`merge`-Validierung. Board-Identität durchgängig auf global eindeutige `projectId` (statt owner-relativer `number`). 5-Agenten-Review → HIGH/MED/LOW-Findings gefixt. Gates grün: 2363 vitest, 458 Rust-Tests, clippy/fmt, tsc, build. **Offen:** `.exe`-Smoke durch User (Org-Board ARO-LABS als globales Board laden).

## Nächste Phase

- [ ] **Offen** — nächstes Vorhaben planen. Kandidaten: (a) Tier-3-Refactors aus dem Review (settingsStore 1264-Z.-Split, create_session 344-Z.-Extraktion) als dedizierte, getestete Efforts; (b) `vitest` 3→4 Major-Migration (schließt die 2 Dev-Tooling-Criticals aus `npm audit`); (c) MCP-Integration für Tasks reaktivieren.

## Backlog

- [ ] chore(ui/store): **Review-Follow-ups aus PR #39/#40** (alle non-blocking, verifiziert 2026-07-16) — (a) Copy-Feedback-Mechanik 3× dupliziert (AboutPanel 2000ms/Check, KnowledgeSection 1500ms, `CopyableCommand` in KanbanBoard) → shared Komponente/`useCopyFeedback`-Hook in `shared/`, inkl. Timer-Cleanup; (b) Shell-Whitelist 4× handgepflegt (Typ-Union, migrate-Inline, `SHELL_PREFS` in wireRuntimeGates, `FALLBACK_SHELL_OPTIONS` im Panel) → exportiertes `sanitizeDefaultShell`; (c) `sound.volume` einziges numerisches Setting ohne `sanitizeXxx`-Clamp (Trust-Boundary + migrate/merge/onRehydrate heilen nicht); (d) `applySettingsSync` nicht compile-time-exhaustiv → `Required<SettingsSyncPartial>`-Fixture-Test oder Handler-Map; (e) `applySettingsSync` in eigenes Modul extrahieren (entschärft Mock-Drift in `wireRuntimeGates.test.ts`: Mock exportiert `sanitizePermissionMode` nicht); (f) herausgelöst als eigener Punkt (s. u., `claude_missing`-Hint); (g) Picker-Render-Pfad von `CopyableCommand` ungetestet.
- [ ] chore(ui): `claude_missing`-Hint trägt den Install-Befehl weiter als Fließtext statt strukturiertem `command`-Feld — bewusst NICHT mit #38 erledigt (vormals Follow-up-Punkt (f) aus PR #39/#40): der Hint rendert in Toasts (Restore-All-Failed, Session-Start), die keine `CopyableCommand`-Darstellung haben; braucht erst ein Command-Rendering im Toast oder einen anderen Anzeigeort. Aus PR-#47-Review entkoppelt (2026-07-21).
- [ ] chore(ui/store): **Review-Follow-ups PR #44** (Session-Neustart, non-blocking) — (a) `spawnFreshSession`-Extraktion: der create+addSession+Error-Toast-Block existiert 4× nahidentisch (useSessionCreation ×3 + sessionRestart); (b) Orphan-PTY-Klasse: close-Fehler auf lebender Session → alter Prozess läuft headless weiter (gilt auch für `SessionList.handleClose`, fire-and-forget — Neustart loggt das inzwischen ehrlich, killt aber nicht); (c) History-Override-Duplikat: Neustart übernimmt den Titel → `sessionTitleOverrides` bekommt einen zweiten identischen Eintrag für die neue UUID.
- [ ] **Meilenstein „Enterprise-Ready"** (GitHub Milestone #1, angelegt 2026-07-10) — 10 Issues, priorisiert Tier 1→3: #22 Windows Code Signing (härtester Blocker), #23 Managed Deployment (MSI/silent/per-machine), #21 Auto-Update-Toggle (umgesetzt (PR #46), Merge nach User-Smoke; Maintainer-Entscheid: Default AN — der Toggle gated nur den automatischen Check, der manuelle v-Badge-Check bleibt immer möglich), #24 Managed Configuration (Admin-Policies, Override zu #21), #25 Proxy-Tauglichkeit (Doku-Teil „Betrieb hinter Corporate Proxy" im README erledigt; reale Proxy-Verifikation offen), #26 Datenfluss-Doku, #27 GHES/SSO (inkl. SAML-Fehlerklassifizierung aus PR-#17-Analyse), #28 i18n/EN, #29 SBOM/Provenance/SECURITY.md, #30 Log-Hygiene, #31 a11y-Audit, #32 Versionspolitik, #34 GitLab-Anbindung (Self-Managed; Provider-Abstraktion, Lane↔Label-Mapping als Design-Schritt, verwandt #27/#33). Updater-nahe Issues (#21/#22/#23/#24/#29) berühren geschützte Pfade → Triple-Check.
- [ ] feat(session): **Agent-Profile — lokale LLMs / alternative Coding-CLIs (#33)** — Session-Kommando konfigurierbar (Claude bleibt Default; OpenCode/Ollama-CLIs/Custom), Feature-Kopplung via Capability-Modell (Library/Resume/Permission-UI degradieren deklariert statt kaputt). **Major-Release-Headline-Kandidat**; Design-Schritt vor Implementierung. Synergie: Enterprise-Meilenstein (Datensouveränität), #24.
- [ ] feat(tauri): `claude --session-id <uuid>` beim Fresh-Spawn evaluieren — SmashQ vergibt die UUID selbst, Discovery/Watcher/Scan-Heuristik entfällt komplett für frische Sessions (echte Determinismus-Lösung für die Session-Identity-Klasse; CLI-Flag-Support + Fehlerpfad prüfen). Aus Session-Identity-Fixes 2026-07-08.
- [ ] fix(tauri): macOS-Aequivalent zum `win_job`-Prozessbaum-Kill — auf macOS ueberleben claude + MCP-Server-Grandchildren einen App-Crash (Job Object ist Windows-only; Kandidat: Prozessgruppen + SIGKILL). Aus Mac-Support 2026-07-02.
- [ ] fix(store): `resetToDefaults` umgeht Rust-Gate-Sync + Cross-Window-Broadcast für `backendFileLogging` (settingsStore.ts, latent — kein UI-Caller). Aus Logging-Review (low).
- [ ] fix(ui): Protokoll-„Löschen" bei **3+** Fenstern — ein frisch mountendes Log-Fenster kann via `log-snapshot-request` eine Snapshot-Antwort von einem Fenster erhalten, dessen `log-cleared`-Handler noch nicht lief, und die gerade gelöschten Einträge re-importieren. Zwei-Fenster-Fall ist dicht (Task 7, Logging-Redesign 2026-07-04). Kandidat: Clear-Generation/Watermark, die Snapshot-Antworten respektieren. Aus finalem Review (important, narrow race).
- [ ] perf(tauri): env_logger-Format-Closure macht sync open/write/flush pro Log-Record auf dem loggenden Thread — Batching/Channel-Entkopplung. Aus Logging-Review (medium, Perf).
- [ ] perf(tauri): Seek-basierter Tail-Read für `read_structured_log` statt Volldatei-Scan (aktuell gebounded VecDeque, aber weiterhin voller Scan). Aus Logging-Review (Optimierung).
- [ ] security(tauri): `validate_id` führenden Bindestrich verbieten — ASCII-only ist erledigt (2026-06-14, `is_ascii_alphanumeric`). Rest: ein alleinstehender `-`/`--`-Prefix kann von gh als Flag interpretiert werden (latente Argument-Injection bei `move_project_item`). Low; Test mit anpassen.
- [ ] refactor(ui): `KanbanBoard.tsx` (778 Z., 2.6x über 300-Limit) splitten — Typen + Cache-Layer in Module, Drag-and-Drop in `useKanbanDragDrop`-Hook. Eigener getesteter Effort (Suite als Netz). Aus Kanban-Delta-Review (medium).
- [ ] chore(ui): Rule-vs-Reality-Drift entscheiden — Design-Regel will `ICONS.*`-Registry, aber 46 Dateien importieren direkt aus `lucide-react`; `ring-2`-Fokus-Regel vs de-facto `ring-1` (14:8) bei Inputs/Selects. Entweder Regel an die Realität anpassen oder codebase-weit migrieren — NICHT punktuell. Aus Kanban-Delta-Review (nit).
- [ ] cleanup(kanban): totes `folder`-Prop aus `KanbanDetailModal`/`IssueCommentForm` entfernen (einziger Caller übergibt immer `null`, Rust-Default-cwd greift). Cross-component + Test-Surface. Aus Kanban-Delta-Review (nit).
- [ ] refactor(store): `settingsStore.ts` (1264 Z.) in Slices/Per-Domain-Stores splitten (Review-Finding #11, behavior-preserving, Tests als Netz). Tier-3, separat geplant.
- [ ] refactor(tauri): `create_session` (344 Z.) in `spawn_reader_thread`/`spawn_waiter_thread`/`spawn_claude_id_watcher`/`build_pty` extrahieren (Review-Finding #12). Tier-3, separat geplant.
- [ ] chore(deps): `vitest`/`@vitest/coverage-v8` 2.1.9→4 Major-Migration (Dev-Vuln GHSA-5xrq-8626-4rwp, die 2 verbleibenden `npm audit`-Criticals), prüft 131 Testdateien gegen die neue Major. Achtung: `overrides`-Eintrag `"vite": "$vite"` aus #16 dabei neu bewerten (vitest 4 bringt eigene vite-Range mit).
- [ ] refactor(tasks): `TaskMetaChips.tsx` (~616 Z.) + `TaskDetail.tsx` (~600 Z.) unter das 300-Zeilen-Component-Limit splitten (behavior-preserving, Tests grün vorher+nachher als Netz + Re-Smoke).
- [ ] refactor(tasks): interne `deadline`-Bezeichner aufs Termin-Modell umbenennen (`DeadlineBucket`/`groupByDeadline`/`classifyDeadline`/`computeDeadlineSeverity`/`TaskDeadlineChip`/`ICONS.tasks.deadline`) — rein mechanisch, kein Verhalten.
- [ ] Tab-Bar-Konfiguration pro Projekt — Revival mit Code-Review-Befunden (Bloat-Reduktion, dnd-kit-Pattern-Split, ConfigPanelTabList-Refactor).
- [ ] feat(library): Inhalts-Badges am zugeklappten Scope-Header — zeigen ohne Aufklappen, was ein Scope enthält. Aus Library-Review 2026-07-02.
- [ ] feat(library): globale Library-Suche über alle Scopes und Kategorien. Aus Library-Review 2026-07-02.
- [ ] feat(library): Override-Kennzeichnung, wenn ein Skill/Agent in global UND Projekt existiert. Aus Library-Review 2026-07-02.
- [ ] feat(library): Einzel-Panel-Refresh statt globalem Neuladen der ganzen Discovery. Aus Library-Review 2026-07-02.
- [ ] feat(editor): Unsaved-Changes-Warnung bei Tab-Wechsel/Close/Datei-Öffnen.
- [ ] feat(editor): Projekt-Dateibrowser für `.md`-Dateien.
- [ ] feat(editor): Library-Integration (Klick auf Datei → Editor öffnet).
- [ ] feat(session): Node/Graph-basierte Session-Visualisierung — Spezifikation nötig (Feature-Politik: Tests + Design-System).
- [ ] feat(ui): Pin-Reordering per Drag & Drop.
- [ ] feat(session): Gamification-System — Spezifikation nötig (Feature-Politik: Tests + Design-System).

---

*Format: `- [ ] Task (#issue)`. Sobald aus dem Backlog Arbeit wird → GitHub Issue anlegen → in "Aktuelle Phase" oder "Nächste Phase" promoten. Historie liegt in `CHANGELOG.md` und git-log.*
